{
  "scripts": {
    "repl": "npm run replit && spago run",
    "replit": "spago bundle-app",
    "start": "node index.js",
    "build": "spago build && spago bundle-app"
  },
  "dependencies": {
    "big-integer": "^1.6.48",
    "chalk": "^4.1.0",
    "cli-highlight": "^2.1.4",
    "diff": "^4.0.2"
  }
}
